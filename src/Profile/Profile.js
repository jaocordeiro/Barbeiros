import React from 'react';
import {Text} from 'react-native';
import {Container} from './Profile.Styled';

export default () => {
  return (
    <Container>
      <Text>Profile</Text>
    </Container>
  );
};
